import type { Polarization } from '../types/game';
export function ElectionWorkshop({seed,setSeed,polarization,setPolarization}:{seed:number;setSeed:(n:number)=>void;polarization:Polarization;setPolarization:(p:Polarization)=>void}){return <section className="panel"><p className="eyebrow">Election Workshop</p><h2>Set up one election</h2><label>Seed <input type="number" value={seed} onChange={e=>setSeed(Number(e.target.value)||1)}/></label><label>Polarization <select value={polarization} onChange={e=>setPolarization(e.target.value as Polarization)}>{['Low','Medium','High'].map(p=><option key={p}>{p}</option>)}</select></label><p>The seed creates one fixed set of ballots. Change settings to make a new classroom experiment.</p></section>}
